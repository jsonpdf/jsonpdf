{
  "name": "@jsonpdf/example-resume",
  "private": true,
  "scripts": {
    "render": "pnpm exec jsonpdf render -t template.json -d data.json -o output.pdf"
  },
  "dependencies": {
    "@jsonpdf/cli": "workspace:*"
  }
}
